# Set these as Worker variables and encrypted secrets, never in public files.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
AUCTION_OWNER_EMAIL=
APP_ORIGIN=
CF_ACCESS_AUD=
# Optional. Keys the per-minute rate-limit hash so stored client keys cannot be brute-forced.
RATE_LIMIT_SECRET=
