import './validate-sponsors.mjs';
import { readFile,writeFile,mkdir,readdir,cp } from 'node:fs/promises';
import { build } from 'esbuild';
const types={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8','.mjs':'text/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.svg':'image/svg+xml','.png':'image/png'};
const assets={};async function collect(dir,base=''){for(const e of await readdir(dir,{withFileTypes:true})){const rel=base+'/'+e.name;if(e.isDirectory())await collect(dir+'/'+e.name,rel);else{const binary=rel.endsWith('.png');assets[rel]={body:await readFile(dir+'/'+e.name,binary?'base64':'utf8'),encoding:binary?'base64':'utf8',type:types[rel.slice(rel.lastIndexOf('.'))]??'text/plain'}}}}
await collect('public');await writeFile('server/assets.generated.mjs','export const assets='+JSON.stringify(assets)+';');await mkdir('dist/server',{recursive:true});await build({entryPoints:['server/worker.mjs'],outfile:'dist/server/index.js',bundle:true,format:'esm',platform:'browser',target:'es2022',minify:true});await mkdir('dist/.openai',{recursive:true});await cp('.openai/hosting.json','dist/.openai/hosting.json');await cp('drizzle','dist/.openai/drizzle',{recursive:true});console.log('Built Worker, game assets, and database migrations.');
